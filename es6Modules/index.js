/**
 * let's import the required components
 * Need to use object destructuring
 */

import { PI, getCircumference, getArea, getVolume } from "./mathUtil";

console.log(`PI is ${PI}`);
console.log(`Area is ${getArea(2)} cm^2`);
